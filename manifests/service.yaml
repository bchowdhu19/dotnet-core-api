apiVersion: v1
kind: Service
metadata:
  name: autodeployXX
  namespace: namespace-workflow-1708349915184
spec:
  type: LoadBalancer
  selector:
    app: autodeployXX
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000